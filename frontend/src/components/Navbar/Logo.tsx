import { Box, Text } from "@chakra-ui/react";

const Logo = () => {
  return (
    <Box>
      <Text fontSize="lg" fontWeight="bold" color={"teal.600"}>
        LOGO
      </Text>
    </Box>
  );
};

export default Logo;
