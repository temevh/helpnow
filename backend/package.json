{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npx ts-node src/index.ts",
    "dev:db": "docker compose up -d",
    "dev:studio": "npx prisma studio",
    "dev:server": "npx ts-node src/index.ts",
    "dev": "npm run dev:db && npx prisma generate && concurrently \"npm run dev:server\" \"npm run dev:studio\"",
    "stop": "docker compose down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.16.3",
    "apollo-server": "^3.13.0",
    "bcryptjs": "^3.0.2",
    "graphql": "^16.11.0",
    "prisma": "^6.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "concurrently": "^9.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
