Frontend: React Next.js Typescript Chakra UI Tailwind
API: GraphQL, Apollo server
Backend: Next.js routing
Database: Postgresql with prisma 
Mobile: Flutter
Hosting: AWS, vercel

Utils:
Input validation: ZOD
Authentication: next auth
Password hashing: bcrypt
Geocoding: Google maps geocode API

FEATURES:
- chat between (potential) volunteers and post creator
- Convert address to coordinates when submitting post
- When not signed in, show "X people need help in your area" and a call to action

DEPLOYMENT:
Compute: Fargate 
    - Two services:
        - Frontend (next.js)
        - api (apollo server / node.js)

Networking: VPC
    - Application Load Balancer (ALB)
    - Security groups

Database: Amazon RDS Postgresql

Containers: ECR
    - Docker images
    - Repos for frontend and backend

Secrets: Systems Manager Parameter Store
    - DB URL
    - jwt Secrets
    - API keys

Done: 
Backend dockerfile working 

Todo:
Fix frontend build issues...
Get frontend dockerfile working 
Move dockerfiles to ECR


Docker:
- Backend
    docker build -t my-backend .
    docker run --rm -p 5000:5000 --env DATABASE_URL="postgresql://POSTGRES_USER:POSTGRES_PASSWORD@host.docker.internal:5432/POSTGRES_DB"
- Frontend:
    docker build -t my-frontend .
    docker run --rm -p 3000:3000 -e NEXT_PUBLIC_API_URL=http://localhost:5000/graphql my-frontend